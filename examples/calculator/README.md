# Calculator specimen

A small, real JavaScript project with cross-file calls, input validation, branches, and an unresolved built-in call. Atlas indexes this source without executing it.

`node examples/calculator/demo.mjs` runs the separate example assertions. These assertions are not an Atlas runtime trace. Select `calculate` in the Atlas workbench to inspect its transitive lexical call candidates.
