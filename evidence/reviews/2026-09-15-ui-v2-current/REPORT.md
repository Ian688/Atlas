# Atlas UI v2 当前开发验收

结论：**CHANGES_REQUIRED。界面框架和部分真实操作已落地，尚未达到 U1–U6 完整界面交付。** 这次检查的是进行中的实现，不把它描述成开发者已经声称完成的最终版本。

依据：任务书 v5、`docs/design/atlas-v2/` 的页面与按钮契约；当前工作树重新构建的真实产品。浏览器里原先的 8768 页面是设计原型，不计入产品验收。实际检查时间和源码、二进制指纹见 [identity.json](identity.json)。目录日期沿用本轮任务编号，实际时间以该文件为准。

## 需要继续开发的部分

| 优先级 | 实际问题与影响 | 完成方式 |
|---|---|---|
| P1 | 运行 spin 后切到 add，再点“后台任务”，只会进入项目首页，没有运行记录或取消入口。服务端取消有效，但用户离开对象后无法从承诺的入口管理任务。定位：`web/app.js:2169`。 | 展示真实运行的对象、状态、返回运行页及取消按钮；切换选区不丢任务，取消后显示真实终态。 |
| P1 | “项目设置”只有启动参数提示；项目首页没有打开目录、最近项目或切换控件。页面还让用户运行 `bash scripts/demo.sh <目录>`，但该脚本固定打开 examples/tour，不读取这个参数。定位：`web/app.js:2170`、`renderHome`、`scripts/demo.sh:13`。 | 按 U4 做实际项目入口和可生效的测试设置，完成两个项目切换与状态隔离。开发期间先纠正错误启动说明。 |
| P2 | 运行页虽能得到 40，但结果是小号技术段落，输入前也放了长篇执行画像；返回值、异常、日志、历史、比较没有形成设计中的清楚层次。 | 先突出返回值/异常、耗时和本次输入；参数及运行按钮清晰可见。技术依据放详情；完整输入、历史、比较和取消按 U2 接通。 |
| P2 | 修改页仍是旧面板纵向堆放，缺少可选择的提案列表、集中差异和验证证据区。Agent 页每个提案按钮都只切到 review，没有定位对应提案（`web/app.js:2328`）。 | 按 U3 完成提案选择→差异→测试与比较→应用/撤销→更新源码；Agent 入口携带并定位同一提案。 |
| P2 | 3D 新页面目前是说明与旧视图入口；文件成员区只描述当前对象。Agent 页有本地上下文导出，但活动区主要是计数和说明。 | U5 复用已有真实 3D，实现空间选区、文件成员与工作台往返；展示具体提案及其可核对的验证/应用记录。无需新增模型平台。 |

截图：[运行结果](03-run-result.png)、[修改提案](04-review-proposal.png)、[后台任务入口实际落点](05-background-tasks.png)、[项目设置](06-settings.png)、[3D 页面](01-city.png)。这不是颜色或间距的精修要求，而是用户操作和信息层次尚未完成。

## 已独立确认的进展

- 真实 Chrome 中六个页面可进入；搜索 checkout、点击关系中的 add，能切到对应函数与源码。
- 输入 17、23，真实运行返回 40；经界面提交 diff 后能看到登记的提案及差异。
- Agent 页面可以打开真实上下文导出对话框；本项未验证外部 Agent 完整任务。
- 运行中的任务经 HTTP 取消，观察到 `cancelled` 终态。本项确认接口取消，不代替离页后的 UI 管理验收。
- 1600 和 1024 宽度下探索/运行/修改页面没有根页面横向溢出；未发现浏览器 JS 异常。这不等于各断点的完整视觉验收。

`02-explore-selected.png` 在关系跳转加载期间截取，不用其中的加载文字判定源码故障；后续 `07-explore-1024.png` 已显示实际源码。

## 执行证据与边界

| 检查 | 结果 |
|---|---|
| `cargo build --workspace --locked` | exit 0 |
| `node --test web/tests/app.behavior.test.mjs` | exit 0；68 个现有行为检查通过，日志见 web-tests.log |
| 本目录 `probe.cjs` | exit 1；13 项通过、2 项失败，见 results.json、probe.log |
| `git diff --check` | exit 0 |

探针通过真实鼠标/键盘操作导航、搜索、关系、输入、运行、提案、上下文及设置入口；直接 API 仅用于核对和取消自己的后台运行。运行使用临时样本和临时 store，结束关闭自己的浏览器/服务并清理样本。没有修改用户项目源码。

未重新执行完整补丁验证/应用/撤销、重启恢复、最终包仓库外旅程或外部 Agent 任务；这些不标成失败，也不借用旧报告标成通过。新开发证据目录目前只有 U1/U2 脚本与取消结果，没有本轮完整交付报告；U6 尚无可据以验收的本轮交付证据。通过项数量不是产品完成百分比。

## 下一轮交付

沿用 U1→U6，不另起后端专项。保留已工作的框架与真实能力，先收拢运行页和修改页的信息与操作，并补上后台任务入口；随后完成项目设置、Agent/3D。每页先对照设计走一遍用户任务，再进入下一页。最后从新分发包走完整旅程，交一份报告与当前 progress 指针。

本次只增加复审证据并更新交接指针，未修产品代码。旧 S1–S6 的历史自测记录保留，不代表新界面已验收。
