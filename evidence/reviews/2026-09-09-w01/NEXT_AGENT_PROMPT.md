# 可直接转交：Atlas w01 复审整改与持续开发

继续开发 `/Users/yinsijie/CodeRepo/Atlas`。这是独立 Atlas 仓库，不是 Modus 内旧 Atlas。用户继续采用每日最多 10 小时的 Goal 开发窗口；你在自己的平台使用 Goal 模式记录真实开始、截止与中断。已有窗口尚在运行时按实际剩余额度接续，不擅自重置套餐额度；新窗口从真实启动时开始。此提示词不启动外部调度器。

先读 `AGENTS.md`、`README.md`、`docs/ARCHITECTURE.md`、`docs/HANDOFF.md`、`docs/DAILY_DEVELOPMENT_WORK_ORDER.md`、`docs/implementation/progress.json`，然后完整阅读：

`evidence/reviews/2026-09-09-w01/REVIEW.md`

本次独立复审结论是 **CHANGES_REQUIRED**：你的原 9 项命令复跑全部通过，但新增真实链路发现了错误确定值、合法语法索引失败、IR 校验遗漏和 Rust 阶段 deadline 失效。保留已有实现并修复根因，无需重建仓库或破坏式删除。开发前核对当前分支/工作区和复审绑定的源指纹；保留他人改动。不要因尚无 Git HEAD 使用 reset/clean 删除 untracked 文件，也不要切去另一份旧实现。需要建立 Git 基线时，仅纳入已核对的 Atlas 自有文件，排除构建产物和本地敏感数据，不把整库 `git add .` 当作审查。

工作优先级：

1. R1：操作级异常状态，不能把抛出后的赋值送入 catch。
2. R2：已知/未知调用的堆与闭包效果转移，累计 may_call 不得被覆盖。
3. R3：候选对象缺失字段与未知原型不能被忽略。
4. R4：JavaScript primitive 加法语义；普通已知输入必须精确。
5. R7：合法无绑定 catch，不因缺参数拒绝整个项目。
6. R6：重复 binding、悬空引用及逐函数 Flow 覆盖对账。
7. R5：把截止/取消/全局工作预算真正传到 Rust 分析和提交阶段，超时后零成功 Analysis 发布、执行停止、子进程回收。
8. 完成上述闭环后继续 M1 参数常量/函数目标/堆引用/效果代入与调用图重新调度，M2 finally 精度、带类型的值合同，再按依赖推进 W06/W07 等；不以本清单完成为由提前结束仍有有用工作的窗口。

每项实现须先复现，再修复，再锁定正式回归测试。独立 probe 在上述证据目录，目前语义脚本和边界脚本退出码均为 1，是确认的失败，不是环境阻塞。边界脚本含受控缓存 worker，以真实解析材料隔离 Rust 阶段 deadline；不得删除该类测试，仅保留容易通过的 worker timeout。覆盖记录中的缺项有意保留，不能移除难例以获得全绿。

不要通过以下方式消除失败：把普通语义一律改 unknown；删除断言；改 oracle 迎合实现；只修展示文字；把全局 unknown 标记当作值流失效传播；拉长 deadline 或统一超时；将过近似精度损失与漏掉真实结果混为一谈。未支持的复杂动态情况可以有明确理由的 conservative unknown，但 primitive、普通函数调用和正常控制流能力要实质提升。

原始交付与复审证据作为历史保留。正式测试可从样例提炼，原 probe 不改成新的期望以遮盖问题；若合同升级需要调整测试消费者，在新窗口复制并记录调整理由和对应旧用例。新窗口使用独立 evidence 目录，修复后的语义/算法版本和 Analysis 身份更新；旧 Snapshot/Analysis/facts 不原地篡改。

每完成一个可验证切片就继续下一个，允许合理调换顺序。保留约 45–60 分钟最终收敛；不能在仍剩约 7 小时时以“下一个完整功能太大”停止。当前缺失的测试、契约防错、取消恢复、性能测量都是可独立推进的工作。不要 sleep 凑时长；如果平台限额、外部必需输入或用户停止导致中断，如实记为中断/阻塞，不声称完成时长。

最终交付必须包括：真实起止时间与剩余额度；R1–R7、M1–M3 逐项状态及证据；正式测试与新增相邻扰动；原验证命令及全部最终退出码；超时/取消后的发布与资源清理证据；覆盖 untracked 的完整 diff/源码清单；最终验证后未变动的产品源码 SHA-256；仍存在的能力边界。更新台账时将实现、自测、资格、独立验收分开，解决覆盖表的重复/矛盾。自测通过后只能申请 `NEEDS_INDEPENDENT_REVIEW`，不得自行标记独立 ACCEPTED。

现在开始实际开发，先复现本轮失败并进入第一项修复；不要只回复计划。
