# 下一单：完成首版交付的最后实际断点

在 /Users/yinsijie/CodeRepo/Atlas 延续现有实现，遵循当前 docs/DAILY_DEVELOPMENT_WORK_ORDER.md D0→D4。先读同目录 REPORT.md 与 distribution-results.json。旧 R1–R4 已在本次 9 项复验内通过，不重新开发。用户要的是可交付产品，本单修通实际分发路径，不开展新增视觉/算法功能。

按以下顺序直接实现，完成一个继续下一个：

1. **L1 修正资源定位。** 复制后的 start.sh 从任意 cwd 启动，初次 index 成功但 HTTP 补丁验证报 worker_missing。修 scripts/dist.sh 生成入口、server_verify_options 及需要的服务配置，让后台验证/重试使用交付包实际 worker。采用最短可靠方案即可。复制包到含空格的新目录，从另一个 cwd 实测提案→验证→对照→应用→撤销；核对实际文件字节。不要只在仓库或 dist 目录里跑。
2. **L2 真实重启恢复。** 现有 --port 0 每次换 origin，localStorage 数据取不到，启动 URL 没有旧选区。实现以项目和分析身份保存/恢复选区、任务页签和运行草稿的最小机制；可选合理稳定入口或服务端本地状态，自行决定。覆盖关闭服务/浏览器后重新启动、同名函数不同项目不串状态、源码改变时重定位或明确拒绝；会话令牌按新会话正常建立。只刷新原页面不算完成。
3. **补齐原 D2 测试与输入合同。** 给操作者可用的显式测试 argv 配置入口，启动配置即可；验证在隔离副本执行，页面展示真实命令、退出码、输出/超时及未运行状态，保留既有授权。exec-compare 复用单次运行完整声明输入，至少把已支持的 this/globals 和 args 一起传到两侧，页面能重用/修改该配置；不能默默丢弃已有输入。测试成功、失败各一例，带全局或 receiver 的对照一例。
4. **对齐分发声明并重新打包。** 检查运行需要的 Node 能力与版本，交付说明和启动检查一致；不承诺未验证平台。修正 README/HANDOFF 过时状态，保留真实缺口。分发包内文件与实际验证源一致。
5. **从最终包做一遍完整用户旅程。** tour + 不同结构临时项目：仓库外启动→查找/真实关系点击→源码→实际运行→多行补丁→隔离验证及显式测试→相同完整输入前后对照→授权临时目录应用/撤销并检查字节→停止服务重启恢复→2D/3D 往返。录真实浏览器操作和关键截图，不能靠页面内合成 click 代替本可进行的指针点击。

复用同目录 distribution.cjs 和 regression.cjs，但先复制到新的证据目录，调整 out/平台工具路径后运行，不覆盖本轮结果。当前脚本指向交付二进制，保留此点，不能换成 debug 后宣称包已验收。失败先做区分性观察，不反复重试同一路径。

编辑中跑聚焦检查；最终按当前任务书跑适用综合检查，记录命令/退出码及交付包身份。一份简洁报告+progress 当前指针即可。自测完成仍标 NEEDS_INDEPENDENT_REVIEW，独立结论由复验产生。保留所有现有未提交工作与历史证据。

本单完成线是原 D0–D4 可用首版；以上通过后提交本地交付物路径和最短启动方式供用户试用。无需新增框架、通用状态平台、模型功能或跨平台发布；也不要以“列出下一步建议”代替继续修完可推进的步骤。
