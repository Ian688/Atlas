# UI v2 R1–R4 独立复验

**结论：PASS（本次四项整改范围）。可以关闭上一轮 R1–R4 整改单。** 当前最终包的单项目核心操作、双项目正常切换及本次重启检查通过；不把该结论扩大为整个 U1–U6 或所有平台的完整验收。

## 实际操作与结果

使用自编 [probe.cjs](probe.cjs)，将最终分发包复制到新的临时目录，通过包内 start.sh 在仓库外启动，用真实 Chrome 操作。两个项目均含同名、同基础字节的 math.js，beta 另有 extra.js，沿用上一轮会暴露串项目问题的条件。

| 整改 | 独立复验 |
|---|---|
| R1 写入和重索引跟随项目 | 页面勾选「以可写方式打开」进入 beta；提交并验证 beta 补丁；确认层显示 beta；应用后 beta 字节改变、alpha 保持原样；打开新版本仍在 beta；跨版本撤销后两项目均精确恢复各自原始字节。另以只读方式打开项目，HTTP 应用返回 403。 |
| R2 草稿隔离 | alpha 的 checkout 填 719、83，切到 beta 同名函数时输入为空；返回 alpha 后仍是 719、83。 |
| R3 运行记录身份 | beta 的后台面板将 alpha 任务标注为另一项目/版本；点击查看结果后明确拒绝，未将 alpha 结果装入当前结果卡。 |
| R4 项目设置 | alpha 声明 node --test；首次打开 beta 时无测试声明；给 beta 设置带 BETA-INDEPENDENT 标识的 node -e 命令，beta 验证区出现其自己的命令；返回 alpha 恢复 node --test。停止原服务、重新启动后，两项目各自声明和写目录均正确恢复。 |

同时回归：搜索/关系/源码、checkout(19,4) 实际返回 15、离开函数后从任务面板取消 spin、页面声明测试并真实验证、基线 15/补丁 23 的同输入对照，以及 alpha 单项目应用→新版本→跨版本撤销。

截图：[beta 写入确认](08-beta-write-confirmation.png)、[beta 应用后](09-beta-applied-to-alpha.png)、[新版本仍是 beta](10-reindex-project.png)、[外项目任务明确拒绝](07-foreign-task-refused.png)。部分文件名沿用上一轮探针，文件名不表示本次仍有同名故障；以图内项目和 results.json 断言为准。

## 证据与检查

- 最终探针 **16/16 PASS，exit 0**，见 [results.json](results.json) 和 probe.log；未发现浏览器 JS 异常。
- 现有网页行为测试 **78/78 PASS，exit 0**，见 web-tests.log。
- 当前源码计算出的构建指纹与实际复制包的 /api/report 一致：`2029ab355d686b063563d0585f6722e39897105ede84a9965466325f078c8b52`。包文件哈希、实际时间与身份见 [identity.json](identity.json)、fingerprint-check.json。
- 读取核对实现方 verify 报告为 PASS，指纹一致；本轮没有重复运行整个 verify.py，不将其计成独立全量测试。
- `git diff --check` exit 0，见 diff-check.log。

第一次探针的功能断言已通过，但复验脚本仅发 SIGTERM 未确保旧启动器完全退出，随后启动可能使用回退端口，因此不能作为真正停止再启动的证明。已清理自己的旧进程组，修正脚本停止步骤，再以全新样本完整重跑；第一次记录保存在 attempt1，不用于重启结论。第二次运行真正结束了旧服务，但 goto 同一 origin 下的新 token fragment 只触发页内导航，没有执行 boot，因此重启后的状态等待超时；该次记录保存在 attempt2。最终脚本先结束旧进程组，再进入 about:blank 后加载新会话页面，验证同 store 的持久恢复；未修改产品代码来消除这两个脚本问题。

所有写入均针对本轮临时样本；结束清理自己的包、样本、浏览器和服务。没有修产品代码，没有修改用户项目源文件或历史证据。

## 资格和下一步

本次关闭的是上一轮已复现的四项问题，不代表已验证多客户端并发切换、任意提案跨项目写入请求，或穷尽所有版本状态。本轮的重启覆盖设置与写授权；没有新增完整浏览器任务草稿重启资格。

下一步可进入本机试用，并按现有 U5/U6 完成线补剩余独立验收：真实外部 Agent 从接入说明完成任务→人审阅同一提案；实际点击 3D 文件成员返回 2D 并核对对象与版本。无需再为 R1–R4 开一轮重复开发。

界面还有少量旧措辞，例如确认层/审阅区仍把当前写目录描述成「启动时授权的唯一目录」，与已实现的逐项目授权不符；可与 README 当前能力说明一起收尾，不作为本次四项整改的重新阻断。3D 目录过滤、细粒度分析进度、断线自动查记录等保持实现报告中的已知未完成项。
