# Atlas 当前执行任务书

版本：4.0。**这是唯一当前执行队列。** 文件名保留兼容旧链接，不设置每日时限。最新用户指令优先；[产品目标](USE-CASES.md) 决定交付形态，六份规格按任务查阅，不从 W00 重做、不顺着旧 W00–W10 列表机械开发。

可直接转交 [首版交付提示词](START_DELIVERY_AGENT.md)。它是本任务书的执行入口，旧 F1–F5 提示词已由当前交付顺序接替。前端 [主设计](FRONTEND_DESIGN.md) 和 [原型](design/workbench-preview.html) 提供视觉与交互基线，尺寸、组件和实现可调整。

## 1. 产品决定与完整开发顺序

Atlas 面向开发者和 Codex 等编程 Agent，帮助理解项目、验证行为和审阅修改，不限定项目语言。JS/TS 是当前首条已接通的语言链路，不是永久产品边界。首版同时验收人的浏览器流程与 Agent 的工具使用流程；核心能力不依赖模型，Agent 可通过 CLI/HTTP 调用，无需先做专用插件。

### 当前起点

最新已登记交付为 `2026-09-15/d0-d4-first-local-delivery`，独立复验仍为 CHANGES_REQUIRED。旧 R1–R4 在 9 项回归内通过，不从 F1 或 D0 重做。复制包的查找与简单运行通过；仓库外补丁验证失败、真实服务重启丢状态。详见 [最新复验](../evidence/reviews/2026-09-15-local-delivery/REPORT.md)。执行前检查有无更新的代码与报告，已修复直接跳过。

以下 S1→S6 接替旧 D0→D4 顺序。每步完成自动推进下一步；首版完成线包含 S5，不能只交浏览器演示。

### S1 修复分发环境下的修改链

结果：用户从任意目录启动复制后的包，能完成补丁验证。

- 复现 `worker_missing`：初次 index 已用绝对 worker，但 HTTP 验证仍依赖 cwd。统一实际资源定位，使后台验证和重试也找到交付包的 worker。
- 入口：`scripts/dist.sh`、`crates/atlas-app/src/server.rs` 的 server_verify_options、服务启动配置与 worker 路径。
- 完成：包放含空格路径，从另一个目录启动；合法补丁验证成功，失败有明确原因且可重试，授权临时项目应用/撤销后文件字节正确。不要只验证仓库内或 dist 目录内启动。

### S2 修复真实重启恢复

结果：用户关闭服务和浏览器，再启动同一项目，可以继续上次任务。

- 修复随机端口导致 localStorage 换 origin、启动 URL 不带旧选区的问题。按项目及分析身份恢复选区、页签、输入草稿；本地状态存在哪里、如何提供稳定入口自行决定，不另造通用平台。
- 入口：分发启动器、`web/app.js` 状态保存/恢复、服务及本地存储。
- 完成：真正停止服务并重开，新会话可鉴权；选区/页签/草稿恢复。不同项目同名函数不串状态；源码变化时有依据地重定位或说明不能恢复。刷新旧页面不算完成。

### S3 补齐验证与对照

结果：人和 Agent 都能知道修改是否满足声明的输入和测试。

- 给本机操作者显式测试 argv 配置入口，启动配置即可；在隔离副本执行，显示命令、输出、退出码、超时及未运行状态，不需要新增任意 shell 输入页面。
- 前后对照复用单次运行已支持的完整声明，至少包含 args、this、globals。两侧固定各自代码版本，保留运行授权和拒绝原因，不默默丢输入。
- 入口：`server.rs` 的 patch_verify/exec_compare、`patchwork.rs`、`runner.rs`、`web/app.js` 的运行与补丁面板。
- 完成：测试成功/失败/未运行可区分；带 this 或 globals 的函数两侧使用同一配置；测试失败可读，不能伪装成验证全部通过。应用后可查看更新分析，撤销可核对真实字节。

### S4 从最终交付包验收开发者全程

结果：用户按简短说明即可使用，不需要开发者代为修路径或填内部字段。

- 对齐 Node 版本/实际运行能力检查、已验证平台、资源清单和 README；重新生成本机分发包。无需先做跨平台签名或自动更新。
- 用 `examples/tour` 和不同结构临时项目，在仓库外完成：打开项目→查找→点击真实关系→源码/值/未知定位→输入运行→多行补丁→隔离验证及声明测试→前后对照→授权应用/撤销→服务关闭重开→2D/3D 往返。
- 浏览器用真实指针/键盘操作；检查截图中的连线、长 diff、滚动、按钮反馈。验证等待时切换对象不串结果，失败可继续；长分析可取消，作业有终态。
- 完成：这条旅程通过且记录来自最终包。原型截图、接口脚本、页面刷新各不能代替相应真实操作。保留源码与包指纹；同机测试不称干净机器测试。

### S5 验收真实 Agent 使用

结果：一个没有读过开发聊天的编程 Agent，仅靠简短接入说明与 Atlas 的公开工具，就能完成一次代码修改任务；开发者能审阅相同版本、提案和验证记录。

- 先写一份短接入说明：如何启动/连接、发现接口、查询函数/关系/源码、取有界上下文、提交 diff、触发验证并读结果。复用 CLI/HTTP 和 `GET /api/contract`；不依赖读取 Atlas 内部数据库。已有入口在 `docs/HOST_API.md`、`crates/atlas-app/src/agent.rs`、`server.rs`、`crates/atlas-engine/src/bridge.rs`。
- 在自建或明确获授权的临时样本上，给另一个真实 Agent 一个行为目标及验收条件，例如修复金额边界处理并保留正常情况。不给现成补丁或固定工具调用脚本。允许使用当前会话子 Agent 做这项独立验收，不要求另建插件或模型平台。
- Agent 通过 Atlas 找到目标、读取关系与上下文、作出修改提案、获取修改前后运行与测试结果、必要时修正提案，最后给出版本、提案及验证记录引用。目标代码的探索和修改验证通过 Atlas，不能绕过它直接读写样本文件后宣称工具可用；独立验收者可以读文件核对结果。
- 人通过工作台定位同一提案与结果，在授权临时目录应用/撤销。如果 CLI 与页面作者或任务归属使记录不可见，修复正式协作链，不另造示例专用旁路。Agent 实际调用、错误恢复和最终结果留证，不能只录模型叙述或脚本模拟。
- 完成：真实 Agent 任务成功、独立检查确认行为符合要求、工作台可审阅对应证据。源码/上下文只在用户授权范围提供给 Agent，不借验收上传无关项目；无可用真实 Agent 环境时先完成其他工作并明确保留 S5 未验收，不能用接口存在替代。

### S6 交付试用版

结果：用户得到能用的包、明确的用法与可信的支持范围。

- 对最终源码运行适用综合检查；若 S5 改了产品，更新包并复验受影响旅程，不能用旧包证据证明新实现。
- 交付本地包路径、最短启动命令、开发者使用说明、Agent 接入说明、截图和实际限制。一份报告区分 S1–S6 的通过/失败/未验证，更新当前指针；自测通过仍待独立复审。
- 人的旅程和 Agent 旅程缺一，不称首版完成。无新问题时不反复扩充检查，也不把低影响美化作为交付前置条件。默认本地交付，不自动公开发布。

### 首版之后的顺序

1. 根据开发者和 Agent 实际试用暴露的问题改善可读性、错误恢复和任务效率。
2. 扩展下一条语言链路：根据实际使用项目选语言，复用统一项目/函数/关系/运行/修改接口；对新增语言明确区分分析与运行支持。届时确定具体语言，不预设 Atlas 永久只支持 JS/TS，也不先建设“支持一切”的框架。
3. 围绕真实任务增强执行轨迹、上下文和分析精度，随后推进大仓库资格、Modus 等宿主集成及跨平台分发。若其中一项实际阻塞当前用户任务，可提前做最小必要部分。

这是后续方向，不要求本次执行者在 S6 后无限扩张范围。首版交付后，根据试用反馈确定下一单。

## 2. 开工：少读、先跑、找到真实断点

```sh
git status --short
git rev-parse HEAD
```

读 [HANDOFF](HANDOFF.md) 与 [progress.json](implementation/progress.json) 的当前动作，核对相关源码；历史记录不必通读。先实际操作下列 T1 路径。已经能用的步骤直接复用，从第一个失败或缺失步骤开工，别重复建设。

必要时安装/构建；依赖已就绪不必重复安装：

```sh
npm ci --prefix workers/typescript --ignore-scripts --no-audit --no-fund
cargo build --workspace --locked
ATLAS_DEMO_STORE="$(mktemp -d /tmp/atlas-t1.XXXXXX)" bash scripts/demo.sh
```

demo 命令持续运行，打印本地地址；在另一终端/浏览器验证，结束后停止自己启动的服务。使用独立 store 避免与其他会话互扰。地址含会话令牌，只本地使用，交付日志和截图移除令牌。此启动方式是现有开发入口，不是最终安装体验。

开工更新只需说清：本次用户结果、当前断点、接下来修改的位置。无需先写新规划或建立新框架。

## 3. T1 逐步验收与默认实现路径

| 操作 | 应看见的结果 | 优先检查的现有入口 |
|---|---|---|
| 从页面查找一个函数，包括不在第一页的函数 | 名称和文件可区分；有明确空结果/加载失败；可选中 | `web/app.js`、`web/index.html`、`crates/atlas-engine/src/query.rs` |
| 选中并沿关系点击相邻函数 | 标题、源码、关系和输入面板指向同一对象；显示真实邻居，异步旧结果不覆盖新选区 | `web/app.js`、`web/layout.js`、`crates/atlas-app/src/server.rs` |
| 查看关键值来源与未知条目 | 结论易读；有来源则定位其源码区间，无事实则说清原因；已知部分仍可用 | `web/app.js`、`web/style.css`、`crates/atlas-engine/src/query.rs` |
| 打开运行入口并填写输入 | 所选目标明确，输入/所缺条件可编辑或有具体说明；不把内部字段原样堆给用户 | `web/app.js` 的 profile/exec 消费路径、`server.rs` |
| 执行可运行样本，再执行失败样本 | 真实返回或异常；拒绝/超时/取消与成功不同；可查看输入和对应源码 | `scripts/test_execution.py` 对应产品路径 |
| 切换函数或返回前一视图，再导出上下文 | 旧结果不冒充新对象，导出固定到所选版本与实体 | `web/app.js` 的 selection/context 路径 |

上述是验收目标，不声称这些步骤现在都可用。先读已有 handler 和请求，再改代码。默认复用现有选区、接口、运行及补丁链；确实不适用时可以替换并迁移调用者。不要新增一条与现有事实脱节的演示链。

底层精度缺口只在影响本任务判断时修复。对不支持的动态行为提供明确边界；对已承诺支持的普通语义修实现，不把它全改成 unknown 来通过测试。设计选择以能读懂、能完成操作为准，不以卡片数、玻璃效果或模块数抵扣用户任务。

T2 接续入口：`web/app.js` 的 `renderPatches` / `proposePatch` / `writePatch`，`server.rs` 与 `scripts/test_patch.py`。先核对已有 HTTP/CLI 能力，补齐页面到真实验证结果的缺口，再做前后对照。涉及写入继续使用既有授权边界。

T3 接续入口：`scripts/demo.py` / `scripts/demo.sh`、`web/app.js` / `web/city3d.js` 的选区和恢复路径、`web/hierarchy.js`。先复现“关闭再打开”和“2D/3D 往返”丢了什么状态，再补最小持久状态。

## 4. 快速执行规则

- 优先解决主路径上第一个阻塞。常规实现、重构、布局库、参数和辅助依赖自行决定；把选择和理由写一句即可。
- 每项底层工作关联到一条验收结果。非阻塞美化、通用框架、遥远规模优化写入待办后回主线，不顺手扩散。
- 同一失败连续两次尝试没有新证据：缩成最小复现，列出尚未排除的原因，用一次能区分原因的观察再决定修改。不要只重跑、加超时或继续猜。
- 方案过重时先找现有通路或成熟组件。无需为了保留旧实现承担不必要复杂度；也无需为简单页面改动重构整个引擎。
- 发现外部阻塞，记录失败和解锁条件，推进独立步骤。只有所有当前可推进工作都依赖外部条件时才结束并说明；缺少某个浏览器驱动不是“所有浏览器都不能点击”的证明。
- 每完成一个可用步骤简要报结果和下一个阻塞。报告、测试和整理服务于开发，不作为反复绕开的主任务。

## 5. 验证：迭代时小而准，交付时查完整路径

按改动选检查，不要求每次编辑跑全套，也不固定测试数量：

| 修改范围 | 快速检查入口 |
|---|---|
| 2D 页面交互 | `node web/tests/app.behavior.test.mjs` |
| 布局/层级/3D | `node scripts/bench_view_layout.mjs`、`node web/tests/city3d.behavior.test.mjs`；选受影响项 |
| worker 语义 | `npm test --prefix workers/typescript` 与对应语义反例 |
| Rust | `cargo test --workspace --locked <相关测试名>`；涉及新二进制先构建 |
| HTTP/完整查询 | `python3 scripts/test_integration.py` |
| 运行/补丁 | `python3 scripts/test_execution.py` / `python3 scripts/test_patch.py`；选受影响链路 |
| 文档 | 链接/命令/状态一致性、`git diff --check`；无需重跑产品测试 |

UI 交付实际启动页面并通过可用的浏览器自动化、CDP、Playwright 或人工完成操作；记录操作和结果，截图辅助判断布局。DOM 模拟、接口调用和深链接截图各有用途，不能代替实际点击路径。工具不可用时保留这一项未验证，继续可做的实现与检查。无需为采用某个驱动先开发通用测试平台。

产品阶段交付前运行现有综合检查（同一批未再改变的源码只跑一次；失败或后续修改才补相关复验）：

```sh
python3 scripts/verify.py --label <本次标签> --out evidence/development/<唯一目录> --keep-going
git diff --check
```

保留最终退出码、真实失败和检查范围。新功能新增能发现真实回归的关键检查，复用现有入口，不写只复述实现的测试。发布环境/规模资格按声明范围单独验证，不拿全规格门槛阻止 T1 开发，也不拿 T1 通过冒充完整产品合格。

## 6. 交付与接续：只维护一个断点

一份 [简短报告](implementation/DAILY_REPORT_TEMPLATE.md) 加 progress 当前动作即可：用户新增能力、改动、验证与退出码、剩余阻塞、下一步。源码身份引用实际 HEAD + diff/新增文件；不要另造重复台账。报告放新的证据目录，历史结果不覆盖。

自测通过标为 `NEEDS_INDEPENDENT_REVIEW`；复审结论由实际复审产生。未做部分写清，避免“整体完成”。复审等待不阻止独立的已授权后续任务。

Git 提交按当前授权执行，不强制每个小步骤提交，也不要求整个共享工作树干净。只处理自己负责的改动，不自动 push、发布或清理他人文件。

## 7. 只保留必要边界

本地解析不执行用户代码或调用模型；运行和写入按授权进行。本地数据不默认外传。源码版本/对象身份明确，发布事实不可暗改，未知与覆盖可见，工作有界可取消，静态/观测/意图区分。除这些产品边界和用户明确决定外，旧路径、阶段编号、数值默认值和工具经验均可调整。
