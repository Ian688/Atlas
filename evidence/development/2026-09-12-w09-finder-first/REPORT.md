# W09：入口排到最上面（修上一轮的截图发现）

窗口：`2026-09-12/w09-finder-first`
结论：**DELIVERED**。修的是上一轮**从真实截图里学到的唯一一件事**：功能存在但看不见。

## 1. 改动

左栏重排：**「找函数」在最上**（未加载状态 + 名称输入框 + 加载按钮），**「项目清单」收进可折叠区**（`<details>`，默认折叠，计数仍常驻标题）。

原因是上一轮截图暴露的：finder 被排在长长的文件树下面、掉出可视区——**元素在、逻辑验证过，但没人能看见**，而它恰恰是进入其余全部能力的第一个入口。

## 2. 验证

- 截图（真实页面、真实 rxjs 分析）：`找函数 / 未加载` + `输入函数名，例如 isFunction` + `加载全部函数清单` 全部位于首屏可视区，`项目清单 100 / 8938` 折叠在其下；
- `node --check` 通过；`web-behaviour` **44/44**；`city3d-behaviour` 28/28；
- 门禁：**25/25 检查 exit 0**，受控负对照 red（exit 1），
  指纹配对一致 `541a1498f60da2b52b00b9b3c4af7a5fe51b3367abb41a69b670c2aae5ed08b3`，`sources_changed_during_run: 0`。

## 3. 没有做的事

- finder **加载中 / 加载完成 / 搜索命中 / 选中后**的状态本轮仍无截图（无头浏览器点不了按钮）；
  上一轮已用真实接口验证翻页逻辑（18 页 / 8938 对象 → 6573 函数，穷尽），但那验证的是逻辑，不是画面；
- 块 × 绑定状态矩阵仍未提到函数工作台主视图；
- 「167 处未知区域」仍是数字，不可点开。
