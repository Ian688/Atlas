(function(){
const esc=t=>String(t??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const init=s=>s.review||(s.review={tab:'diff',file:'money',status:'verified',compare:true,applied:false,proposal:'boundary',args:'[100,{"rate":1.5}]',before:-50,after:0});
const css=`<style>.rv-layout{display:grid;grid-template-columns:205px minmax(0,1fr) 290px;gap:18px}.rv-list{display:flex;flex-direction:column;gap:10px}.rv-layout>*{min-width:0}.rv-proposal{display:block;white-space:normal;width:100%;text-align:left;padding:18px;border-radius:10px;background:#fff}.rv-proposal.active{border-color:#378b72;background:#edf5ef}.rv-proposal strong,.rv-proposal small{display:block;margin-bottom:8px}.rv-diff{font:12px/2.2 ui-monospace,monospace;white-space:pre;overflow:auto;margin:0;padding:18px 0;background:#fafbf8}.rv-diff span{display:block;padding:0 18px}.rv-del{background:#fff0eb;color:#a44840}.rv-add{background:#e5f3e8;color:#246942}.rv-file{padding:13px 18px;background:#f4f6f2;border-block:1px solid var(--line);font:11px ui-monospace,monospace}.rv-step{display:flex;gap:10px;padding:12px 0;border-bottom:1px solid var(--line)}.rv-dot{height:22px;width:22px;flex-shrink:0;border-radius:50%;background:#e5f1e9;color:#236552;display:grid;place-items:center;font-size:11px}.rv-result{padding:14px;background:#f1f5ef;border-radius:8px;min-width:0}.rv-result strong{font:30px ui-monospace,monospace}.rv-footer{display:flex;justify-content:space-between;align-items:center;gap:15px;padding:18px 20px;border:1px solid #dce7df;border-radius:10px;background:#edf5ed;margin-top:18px}.rv-tabs{display:flex;gap:8px;padding:15px 18px}.rv-small{font-size:11px;line-height:1.8}.rv-evidence>section{padding:18px}.rv-evidence .panel-head{padding:0 0 12px}.rv-evidence .grid2{gap:8px}.rv-caption{font-size:10px;color:var(--muted);margin-bottom:8px}.rv-alert{background:#fff3df;color:#865a20;padding:12px;border-radius:8px;font-size:12px;line-height:1.7}.rv-states{margin-top:18px;display:flex;gap:6px;align-items:center;flex-wrap:wrap;font-size:10px;color:var(--muted)}.rv-states button{font-size:10px;padding:5px 8px}.ag-layout{display:grid;grid-template-columns:minmax(0,1.45fr) minmax(280px,1fr);gap:20px}.ag-layout .panel{padding:23px}.ag-layout .panel-head{padding:0 0 15px;margin-bottom:18px}.ag-item{border-left:2px solid #b8cdc0;padding:0 0 20px 20px;margin-left:7px}.ag-item:last-child{padding-bottom:0}.ag-item time{font-size:10px;color:var(--muted)}.ag-item h4{margin:10px 0 4px}.ag-field{width:100%;box-sizing:border-box;border:1px solid #d9ded5;border-radius:8px;background:#fff;padding:12px;font:13px/1.7 inherit}.ag-note{border:1px dashed #c9d9cc;border-radius:9px;padding:18px;font-size:12px;line-height:1.8}.rv-link{color:#197061;cursor:pointer;background:none;border:0;text-decoration:underline}@media(max-width:1200px){.rv-layout{grid-template-columns:185px minmax(0,1fr)}.rv-evidence{grid-column:2;display:grid;grid-template-columns:1fr 1fr}.ag-layout{grid-template-columns:1fr}.rv-proposal{padding:12px}}@media(max-width:700px){.rv-layout{grid-template-columns:1fr}.rv-evidence{grid-column:auto;display:flex}.rv-list{flex-direction:row;overflow:auto}.rv-proposal{min-width:175px;width:175px;flex-shrink:0}.rv-footer{flex-wrap:wrap}.rv-tabs{flex-wrap:wrap}.ag-layout{grid-template-columns:1fr}.ag-layout .grid3{grid-template-columns:1fr 1fr}.rv-footer .toolbar{width:100%}}</style>`;
const btn=(action,label,c='',attrs='')=>`<button class="btn ${c}" data-action="review:${action}" ${attrs}>${label}</button>`;
const diff=r=>r.proposal==='format'?'<span>  export function formatMoney(amount) {</span><span class="rv-del">−   return String(amount);</span><span class="rv-add">+   return amount.toFixed(2);</span><span>  }</span>':r.file==='test'?'<span>  test("discount beyond amount", () => {</span><span class="rv-add">+   assert.equal(charge(100, { rate: 1.5 }), 0);</span><span class="rv-add">+   assert.equal(charge(100, { rate: 0.2 }), 80);</span><span>  });</span>':'<span>  1  export function charge(amount, { rate = 0 } = {}) {</span><span>  2    const discount = clampRate(rate);</span><span class="rv-del">− 3    return Math.round(amount * (1 - discount));</span><span class="rv-add">+ 3    return Math.max(0, Math.round(amount * (1 - discount)));</span><span>  4  }</span>';
const stateTools=()=>`<div class="rv-states">原型状态：${[['verified','测试通过'],['unrun','未验证'],['failed','测试失败'],['notests','未配置测试'],['error','加载失败'],['empty','无提案']].map(([v,t])=>btn('state',t,'subtle',`data-state="${v}"`)).join('')}</div>`;
window.AtlasPages=window.AtlasPages||{};
window.AtlasPages.review={title:'修改审阅',subtitle:'看清源码差异，核对测试和行为，再决定应用。',render(s){
const r=init(s),status=r.status,id=r.proposal==='format'?'P-103':'P-104',files=r.proposal==='format'?1:2;
const statusText={verified:'测试通过',verifying:'验证中',unrun:'未验证',failed:'测试失败',notests:'未运行测试'}[status]||'待验证';
const head=`<div class="toolbar" data-region="C1" data-region-label="版本与提案入口"><span class="chip">Papertrail</span><span class="muted">基线 v18 · ${r.applied?'已应用修改':'尚未写入项目'}</span><span class="spacer"></span>${btn('new','＋ 粘贴 diff')}<button class="btn" data-go="agent">Agent 交接 ↗</button></div>`;
if(status==='empty'||status==='error')return css+head+`<div class="panel empty" style="margin-top:20px"><h2>${status==='empty'?'还没有修改提案':'修改列表加载失败'}</h2><p>${status==='empty'?'提交一份 diff，或把上下文交给外部 Agent。':'没有丢弃已保存的提案。恢复连接后重新读取。'}</p>${btn(status==='empty'?'new':'restore',status==='empty'?'粘贴第一份 diff':'重新加载','primary')}</div>`+stateTools();
return css+head+`<div class="rv-layout" style="margin-top:18px"><aside data-region="C2" data-region-label="提案列表与来源"><div class="eyebrow">修改提案 · 2</div><div class="rv-list">${[['boundary','修复折扣金额下限','charge · 2 个文件','P-104'],['format','统一金额显示','formatMoney · 1 个文件','P-103']].map(([v,t,n,p])=>btn('select',`<strong>${t}</strong><small>${n}</small><span class="chip">${v===r.proposal?(r.applied?'已应用':statusText):'查看提案'}</span><p class="muted rv-small">${p} · 基于 v18<br>${v==='boundary'?'外部 Agent · 会话声明':'手动导入'}</p>`,`rv-proposal ${v===r.proposal?'active':''}`,`data-id="${v}"`)).join('')}</div><p class="muted rv-small" style="margin-top:18px">选择一份提案独立验证和应用。联合修改需要合并提案。</p></aside><section class="panel" data-region="C3" data-region-label="多文件差异与静态影响"><div class="panel-head"><div><div class="eyebrow">${id} · 修改提案</div><h2 style="font-size:19px;margin-bottom:0">${r.proposal==='format'?'金额统一显示两位小数':'折扣超过 100% 时，返回 0'}</h2></div></div><div class="rv-tabs">${btn('tab','文件差异',r.tab==='diff'?'primary':'subtle','data-tab="diff"')}${btn('tab','静态影响',r.tab==='impact'?'primary':'subtle','data-tab="impact"')}</div>${r.tab==='impact'?`<div class="panel-body"><span class="chip">静态分析</span><h3 style="margin-top:20px">${r.proposal==='format'?'formatMoney':'charge'} 的实现发生变化</h3><p class="muted">${r.proposal==='format'?'金额显示从一般字符串变为固定小数位。当前样例没有列出调用者。':'当前样例的调用者 checkout 可能受影响。关系不代表测试覆盖，也不证明每条路径都运行过。'}</p>${r.proposal==='format'?'':btn('explore','查看 charge 的关系图 →')}</div>`:`<div class="toolbar" style="padding:0 18px 15px">${btn('file',r.proposal==='format'?'format.js':'money.js',r.file==='money'?'selected':'','data-file="money"')}${r.proposal==='format'?'':btn('file','money.test.js',r.file==='test'?'selected':'','data-file="test"')}</div><div class="rv-file">${r.proposal==='format'?'src/pricing/format.js':r.file==='test'?'tests/money.test.js':'src/pricing/money.js'}</div><pre class="rv-diff">${r.importedDiff?esc(r.importedDiff):diff(r)}</pre><div class="panel-body"><p class="muted rv-small">− 删除　＋ 新增 · v18 → ${id} 候选代码</p>${btn('raw','展开完整差异','subtle')}</div>`}</section><aside class="stack rv-evidence" data-region="C4" data-region-label="验证证据与前后对照"><section class="panel"><div class="panel-head"><h3>验证结果</h3><span class="chip ${status==='verified'?'green':status==='failed'?'red':'amber'}">${statusText}</span></div>${[['建立隔离副本','原项目不受影响'],['重新分析修改','检查代码关系变化'],[status==='notests'?'没有运行测试':status==='failed'?'测试未通过':status==='verifying'?'正在运行测试…':status==='unrun'?'等待运行测试':'测试全部通过',status==='verified'?'退出码 0 · 示例结果':'node --test']].map(([title,n],i)=>`<div class="rv-step"><span class="rv-dot">${status==='unrun'?i+1:i===2&&status!=='verified'?'!':'✓'}</span><div><strong>${title}</strong><div class="muted rv-small">${n}</div></div></div>`).join('')}${status==='failed'?'<p class="rv-alert">边界测试预期 0，实际 −50。保留提案并查看错误输出。</p>':status==='notests'?'<p class="rv-alert">隔离分析完成，但未配置测试命令。这不等于测试通过。</p>':''}<div class="toolbar" style="margin-top:14px">${btn('verify',status==='verifying'?'正在验证…':status==='verified'?'重新验证':'开始验证','primary',status==='verifying'?'disabled':'')}${btn('logs','查看输出','subtle')}</div></section><section class="panel"><h3>相同输入，前后比较</h3>${r.proposal==='format'?'<p class="muted rv-small">先验证这份格式修改，再选择它的函数输入。</p>':`<p class="code rv-small">${esc(r.args)}</p>${r.compare?`<div class="grid2"><div class="rv-result"><div class="rv-caption">基线 v18</div><strong style="color:#aa5d44">${r.before}</strong></div><div class="rv-result"><div class="rv-caption">候选 ${id}</div><strong>${r.after}</strong></div></div><p class="muted rv-small">两侧正常返回 · 示例观测</p>`:'<p class="muted rv-small">还没有运行两侧。验证完成后可比较。</p>'}${btn('compare',r.compare?'编辑对照输入':'运行前后对照','',!['verified','notests'].includes(status)?'disabled':'')}`}</section></aside></div><div class="rv-footer" data-region="C5" data-region-label="写入确认与版本接续"><div><strong>${r.applied?'修改已写入 Papertrail':'将这份修改应用到项目'}</strong><div class="muted rv-small">${r.applied?'打开更新后的代码，或撤销本次写入':`${files} 个文件 · ${id} · 操作前再次检查文件是否变化`}</div></div><div class="toolbar">${r.applied?btn('revert','撤销本次修改')+btn('open-version','打开新版本 v19 →','primary'):btn('apply','检查并应用…','primary',!['verified','notests'].includes(status)?'disabled':'')}</div></div>`+stateTools();
},handle(a,el,s,ui){if(!a.startsWith('review:'))return false;const r=init(s),k=a.slice(7);
if(k==='state'){r.status=el.dataset.state;r.compare=r.status==='verified';r.applied=false;s.scenario='ready';ui.render();return true;}
if(k==='restore'){r.status='verified';ui.render();return true;}
if(k==='select'){s.reviewSnapshots=s.reviewSnapshots||{};s.reviewSnapshots[r.proposal]={...r};s.review=s.reviewSnapshots[el.dataset.id]||{...init({}),proposal:el.dataset.id,status:el.dataset.id==='format'?'unrun':'verified',compare:el.dataset.id!=='format'};ui.render();return true;}
if(k==='tab'||k==='file'){r[k]=el.dataset[k];ui.render();return true;}
if(k==='explore'){s.entity='charge';ui.go('explore');return true;}
if(k==='new'){ui.modal('提交修改提案',`<p class="muted">提交仅登记提案，不会改动项目。</p><label>修改目的<input id="rv-purpose" placeholder="一句话说明修改目的"></label><label>统一 diff<textarea id="rv-new-diff" rows="7" placeholder="--- a/src/pricing/money.js&#10;+++ b/src/pricing/money.js"></textarea></label><div class="toolbar">${btn('import','提交并查看','primary')}</div>`);return true;}
if(k==='import'){const d=document.getElementById('rv-new-diff').value;if(!d.includes('--- ')||!d.includes('+++ ')){ui.toast('请填写含文件头的统一 diff。输入已保留。');return true;}r.importedDiff=d;r.status='unrun';r.compare=false;ui.closeModal();ui.render();ui.toast('已保存原型 diff。正式实现将校验全部补丁并展示返回提案。');return true;}
if(k==='verify'){r.status='verifying';r.compare=false;ui.render();clearTimeout(r.timer);r.timer=setTimeout(()=>{r.status='verified';if(s.page==='review')ui.render();ui.toast('隔离验证示例完成，项目尚未修改。');},1100);return true;}
if(k==='logs'){const t=r.status==='failed'?'Tests: 3 passed, 1 failed\nExpected: 0\nActual: -50\nExit code: 1':r.status==='notests'?'没有运行测试\n原因：未配置测试命令':r.status==='unrun'?'尚未启动验证，无输出':r.status==='verifying'?'验证正在执行，尚无最终退出码':'Tests: 4 passed, 0 failed\nExit code: 0 · 820 ms';ui.modal('验证输出 · '+(r.proposal==='format'?'P-103':'P-104'),`<span class="chip">运行观测 · 示例</span><pre class="code">${esc(t)}</pre><p class="muted rv-small">测试命令：node --test。正式输出按返回记录呈现。</p>`);return true;}
if(k==='raw'){ui.modal('完整差异',r.importedDiff?`<pre class="code">${esc(r.importedDiff)}</pre>`:`<pre class="rv-diff">${diff(r)}${r.proposal==='boundary'?diff({...r,file:'test'}):''}</pre>`);return true;}
if(k==='compare'){ui.modal('两侧使用相同输入',`<label>args<textarea id="rv-args">${esc(r.args)}</textarea></label><label>this<input id="rv-this" value="null"></label><label>globals<input id="rv-globals" value="{}"></label><div class="toolbar">${btn('compare-run','运行两侧','primary')}</div>`);return true;}
if(k==='compare-run'){try{const args=JSON.parse(document.getElementById('rv-args').value),receiver=JSON.parse(document.getElementById('rv-this').value),globals=JSON.parse(document.getElementById('rv-globals').value);if(!Array.isArray(args)||typeof args[0]!=='number'||typeof args[1]?.rate!=='number'||!globals||Array.isArray(globals)||typeof globals!=='object')throw Error();r.args=JSON.stringify(args);r.declared={args,this_arg:receiver,globals};r.before=Math.round(args[0]*(1-Math.max(0,args[1].rate)));r.after=Math.max(0,r.before);r.compare=true;ui.closeModal();ui.render();ui.toast('已更新这组输入的前后对照示例。');}catch{ui.toast('检查 JSON 格式；此样例需要 [金额, {"rate": 折扣}]。');}return true;}
if(k==='apply'){ui.modal('确认写入 '+(r.proposal==='format'?'1':'2')+' 个文件',`<p>目标目录</p><pre class="code">/Users/you/Projects/papertrail</pre><p>${r.proposal==='format'?'src/pricing/format.js':'src/pricing/money.js<br>tests/money.test.js'}</p><div class="callout">${r.status==='notests'?'未运行测试。请确认你接受未测试修改的风险；不会显示测试通过。':'已完成隔离验证。'}只应用当前提案，执行前检查文件漂移。</div><div class="toolbar">${btn('confirm-apply',r.status==='notests'?'确认应用未测试修改':'确认应用到此目录','primary')}</div>`);return true;}
if(k==='confirm-apply'){r.applied=true;ui.closeModal();ui.render();ui.toast('应用示例完成，可打开更新后的代码。');return true;}
if(k==='open-version'){s.version='v19';s.entity='charge';ui.go('explore');return true;}
if(k==='revert'){ui.modal('撤销本次修改',`<p>仅恢复本次写入影响的文件。后续编辑存在时停止覆盖。</p><div class="toolbar">${btn('drift','演示文件已变化')}${btn('confirm-revert','确认撤销','primary')}</div>`);return true;}
if(k==='drift'){ui.modal('文件已变化，无法直接撤销',`<div class="rv-alert">src/pricing/money.js 在应用后发生变化。你的当前文件保持原样。</div><p>先检查当前差异，再决定如何保留后续编辑。</p>${btn('explore','查看源码')}`);return true;}
if(k==='confirm-revert'){r.applied=false;s.version='v18';ui.closeModal();ui.render();ui.toast('已恢复应用前内容（原型示例）。');return true;}return false;
}};
  window.AtlasPages.agent={title:'Agent 交接',subtitle:'把代码上下文交给外部 Agent，把它的修改带回同一个工作台。',
    render(s){s.agent=s.agent||{connected:false};return css+`<div class="toolbar" data-region="A1" data-region-label="当前连接与版本"><span class="chip">Papertrail · v18</span><span class="muted">本地接口可用 · 尚未认证外部 Agent 身份</span><span style="flex:1"></span><button class="btn" data-action="agent:connect">连接说明</button></div><div class="ag-layout" style="margin-top:20px"><div class="stack"><section class="panel" data-region="A2" data-region-label="交接目标与上下文"><div class="eyebrow">01 / 准备交接</div><h2>让 Agent 解决一个明确的问题</h2><label class="rv-small">任务目标<textarea id="ag-goal" data-action="agent:goal" class="ag-field" rows="3">${(s.agent.goal||'修复 charge 的折扣边界：当折扣超过金额时，返回 0。通过 Atlas 取上下文、提交提案并验证，先不要应用。').replace(/&/g,'&amp;').replace(/</g,'&lt;')}</textarea></label><div class="toolbar" style="margin-top:14px"><span class="chip">charge</span><span class="code rv-small">src/pricing/money.js</span><button class="btn subtle" data-go="explore">查看上下文</button></div><p class="muted rv-small">交接包含：固定版本、目标函数、上下文获取方式和验收要求。由你交给 Codex 等外部 Agent；Atlas 不会自动调用模型。</p><div class="toolbar"><button class="btn primary" data-action="agent:handoff">预览并复制交接文本</button><button class="btn" data-action="agent:export">导出函数上下文</button></div></section><section class="panel" data-region="A3" data-region-label="提案与可核对证据"><div class="panel-head"><div><div class="eyebrow">02 / 收到修改</div><h2>修复折扣金额下限</h2></div><span class="chip">待人审阅</span></div>${s.scenario==='empty'?'<div class="empty"><p>还没有收到提案。Agent 提交后，相关提案与验证记录会出现在这里。</p><button class="btn" data-action="agent:refresh">刷新记录</button></div>':`<p class="muted rv-small">外部 Agent（会话声明） · P-104 · 基于 v18<br>来源标识不代表已认证的模型或个人。</p><div class="grid3"><div class="rv-result"><div class="rv-caption">修改</div><strong>2</strong><div class="rv-small">个文件</div></div><div class="rv-result"><div class="rv-caption">真实测试</div><strong>4/4</strong><div class="rv-small">通过 · 退出码 0</div></div><div class="rv-result"><div class="rv-caption">观测金额</div><strong style="font-size:24px">−50 → 0</strong><div class="rv-small">相同声明输入</div></div></div><p class="rv-small">Agent 的说明：“增加金额下限，并补充折扣超过金额的测试。”</p><button class="btn primary" data-action="agent:review">审阅这份提案</button> <button class="btn" data-action="agent:evidence">查看证据引用</button>`}</section></div><aside class="stack"><section class="panel" data-region="A4" data-region-label="交接活动与结果"><div class="panel-head"><h3>可核对的活动</h3><button class="btn subtle" data-action="agent:refresh">刷新</button></div><div class="ag-item"><time>10:42 · 提案记录</time><h4>收到 P-104</h4><p class="rv-small muted">修改意图已登记。项目文件尚未改变。</p></div><div class="ag-item"><time>10:43 · 验证记录</time><h4>隔离验证完成</h4><p class="rv-small muted">派生版本 v18-P104<br>测试命令退出码 0</p></div><div class="ag-item"><time>10:44 · 执行记录</time><h4>前后运行均正常返回</h4><p class="rv-small muted">输入 [100, {rate:1.5}]<br>这不是完整执行轨迹。</p></div></section><section class="ag-note" data-region="A5" data-region-label="连接范围说明"><strong>Atlas 提供事实和修改接口</strong><p>此处显示 Atlas 收到的提案与结果，不会推测 Agent 在其他工具中做了什么。尚无完整的外部 Agent 会话日志。</p><button class="rv-link" data-action="agent:contract">查看可用接口</button></section></aside></div><div class="rv-states">原型状态：<button data-action="agent:state" data-state="empty">未收到提案</button><button data-action="agent:state" data-state="ready">已有提案</button></div>`;},
    handle(a,el,s,ui){if(!a.startsWith('agent:'))return false;const k=a.slice(6);
      if(k==='goal'){s.agent=s.agent||{};s.agent.goal=el.value;return true;}
      const handoff='任务：'+(s.agent?.goal||'修复 charge 折扣边界，金额最低为 0。')+' 项目 Papertrail；分析 v18；目标 src/pricing/money.js:charge。先用 Atlas contract/context 读取真实上下文，再提 diff、隔离验证，并报告 proposal/analysis/verification id。不要应用。连接信息由本机操作者单独提供。';
      if(k==='state'){s.scenario=el.dataset.state;ui.render();return true;}
      if(k==='connect')ui.modal('连接本地 Atlas','<p>把启动时生成的本机会话信息交给外部 Agent。接口只在本机访问。</p><pre class="code">ATLAS_URL=http://127.0.0.1:4317\nATLAS_TOKEN=&lt;本机启动会话令牌&gt;</pre><p class="muted">原型不包含真实令牌。实际实现从当前会话取得连接信息，复制时不写入日志或交接记录。</p><button class="btn primary" data-action="agent:copy-connect">复制连接模板</button>');
      if(k==='copy-connect'){navigator.clipboard?.writeText('ATLAS_URL=http://127.0.0.1:4317\nATLAS_TOKEN=<本机启动会话令牌>').then(()=>ui.toast('已复制连接模板')).catch(()=>ui.toast('复制不可用，请手动复制模板'));}
      if(k==='handoff')ui.modal('交接文本 · 复制后交给外部 Agent',`<textarea class="ag-field" rows="8">${handoff.replace(/&/g,'&amp;').replace(/</g,'&lt;')}</textarea><button class="btn primary" data-action="agent:copy-handoff">复制交接文本</button><p class="muted rv-small">这是交接草稿，不代表任务已发送或 Agent 已开始工作。</p>`);
      if(k==='copy-handoff'){navigator.clipboard?.writeText(handoff).then(()=>ui.toast('已复制；请粘贴到外部 Agent')).catch(()=>ui.toast('复制不可用，请手动复制文本'));}
      if(k==='export')ui.modal('函数上下文 · 导出预览','<p>范围：charge 的源码、直接关系、输入要求和未知边界。版本固定为 v18。</p><pre class="code">entity: src/pricing/money.js:charge\nanalysis: v18\ncontext: ctx-demo-104\ncoverage: bounded · 无完整执行轨迹</pre><button class="btn primary" data-action="agent:download">下载上下文 JSON</button><p class="muted rv-small">当前为示例。实现时直接导出 /api/context 返回的有界内容。</p>');
      if(k==='download'){const blob=new Blob([JSON.stringify({demo:true,analysis:'v18',entity:'src/pricing/money.js:charge',note:'原型样例，不是真实项目上下文'},null,2)],{type:'application/json'});const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='atlas-context-demo.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);ui.toast('已下载示例上下文');}
      if(k==='review'){init(s).proposal='boundary';s.entity='charge';ui.go('review');}
      if(k==='refresh'){s.scenario='ready';ui.render();ui.toast('示例记录已刷新；正式页面读取实际提案。');}
      if(k==='evidence')ui.modal('同一份修改的证据','<table class="table"><tr><td>基线分析</td><td>v18</td></tr><tr><td>提案</td><td>P-104</td></tr><tr><td>派生分析</td><td>v18-P104</td></tr><tr><td>验证记录</td><td>verify-104 · completed</td></tr><tr><td>对照运行</td><td>run-before-104 / run-after-104</td></tr></table><p class="muted">示例标识。产品必须使用真实接口返回的记录。</p><button class="btn primary" data-action="agent:review">打开对应提案</button>');
      if(k==='contract')ui.modal('Agent 可用能力','<p>查找和上下文：search / source / reach / context</p><p>修改与验证：patch/propose / patch/verify / exec-compare</p><p>已有 agent/work 只执行有界 inspect、annotate、propose_patch；不运行模型，也不是派单给外部 Agent。</p><p class="muted">完整清单以 GET /api/contract 为准。</p>');return true;
    }
  };
})();
