# 直接交给前端实现 Agent

2026-09-14：本单对应的 F1–F5 已交付。后续统一使用 [首版交付提示词](START_DELIVERY_AGENT.md)，不要再从 F1 重做。下文保留为原设计实施背景。

---

你在 `/Users/yinsijie/CodeRepo/Atlas` 实现 Atlas 正式前端，沿现有 Rust/HTTP/JS 链路交付，不做另一个演示页。

先按 AGENTS.md 了解工程，再打开 `docs/design/workbench-preview.html` 操作原型，读 `docs/FRONTEND_DESIGN.md` 与 `docs/FRONTEND_API_CONTRACT.md` 的当前切片。它们已定义页面、渲染、交互和接线；无需重新决定产品形态或先调研多个前端框架。当前执行顺序仍是任务书 T1→T2→T3，对应主设计 F1→F5。

先交 F1：左侧全局查找 + 中央关系工作区 + 旁侧源码。删除旧首屏重复说明占位，复用现有 selection、layout、source/reach/flow/profile 请求。先实际找到一个不在首屏的函数，再沿真实关系点邻居、返回、快速切换 A/B，确认源码和提案不会串对象。搜索没有服务端支持、旧请求缺 generation 等具体问题按接线表修复，别只改 CSS。

接着做 F2 来源/未知到真实源码区间，F3 按参数输入与真实运行，完成 T1；再做 F4 页面验证和修改审阅、F5 连续工作/结构联动。每片检查现有代码，已工作的部分直接用。不要因 3D 草图出现玻璃就先去打磨材质，也不要因某个未知比例不好看就全面重写求解器。

原型数据只用于布局与交互说明，不能把其中函数名、关系、返回 true、验证状态或数据量写入生产逻辑。正式接口没有的字段见接线表“拟议”；在相关切片补齐。现有观测只够入口结果，不得从静态图生成运行轨迹。原型的“样例状态”选择器和预览提示不进入产品。

原型是视觉基线，不是必须沿用其单文件实现。默认在现有 JS 中提取小模块即可；布局继续复用当前引擎，按真实成本决定何时移 worker。主要动作位置、选区连续性和结论边界要保留；尺寸/组件/代码组织可自行优化。

编辑中跑受影响检查，阶段实际启动服务并通过浏览器点击；不能用原型的 19 项检查证明产品通过。输入格式错误、失败/空结果、长源码、快速切换、返回草稿各覆盖一个关键路径。按任务书运行适用综合检查并记录最终退出码。用一份简短报告和 progress 更新断点，自测通过仍待独立复审。

保留当前未提交文档与其他人的修改。现在先记录 Git 基线，启动现有工作台，定位 F1 第一个断点，直接开始改生产代码。
